<!DOCTYPE html>
<html lang="en">
  <%- include("./../layouts/head",{pageName: "Edit Banner" });%>
  <body>
    <a href="/banners">All Banners</a>
      <a href="/dashboard">Dashboard</a>
    <form method="POST" action="/banners/edit-banner/<%= banner.id%>" enctype="multipart/form-data">
      <input type="hidden" name="id" value="<%= banner.id%>">
      <label for="title">Title</label>
      <input type="text" name="title" value="<%=banner.title%>" required />
      <%= possibleErrors.title %>
      <label for="description">Description</label>
      <textarea
        name="description"
        rows="14" cols="20"
        required
      ><%=banner.description%></textarea>
      <%= possibleErrors.description %>
      <label for="imageUrl">ImageUrl</label>
      <input type="file" name="imageUrl" accept="image/*" required />
      <input type="submit" value="SAVE" />
      <input type="reset" value="Reset" />
    </form>
  </body>
</html>
